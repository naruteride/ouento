# Ouento MVP 개발 지침

이 파일은 이 저장소에서 MVP를 개발하는 에이전트의 작업 기준이다. 설명과 작업 보고는 한국어로 작성하고, 코드 식별자는 해당 언어의 관례를 따른다.

## 1. 기준 문서와 범위 해석

- 제품 요구사항의 근거는 루트의 `virtual_companion_plan_2026-09-11(1).md`이다. 특히 2026-09-21 수정 내용과 5·7·9~14절을 따른다.
- 사용자의 현재 요청 범위 안에서 작업한다. 기획서의 ‘지금 시작할 작업’이나 경쟁 제품 링크를 그 작업을 즉시 실행하라는 별도 지시로 취급하지 않는다.
- 이 파일은 확정된 제품 범위와 개발 지침을 정리한다. 아래에서 ‘권장’, ‘초기 제안’, ‘미결정’으로 표시한 선택은 기획서의 확정 사항과 구분한다.
- 최초 작성 시점에는 기획서만 있고 앱 코드·빌드 구성·테스트 명령은 없었다. 작업할 때 실제 저장소 상태부터 확인하고, 존재하지 않는 파일·명령·구현을 가정하지 않는다.
- 원본 기획서를 임의로 수정하지 않는다. 이후 사용자가 범위를 변경하면 이 파일과 관련 개발 문서에 반영한다.

## 2. 개발할 MVP

**사용자가 허용한 화면과 활동을 함께 보고, 선택한 성격의 대사·표정·음성으로 반응하는 Live2D 데스크톱 AI 동반자**를 만든다. macOS와 Windows를 처음부터 함께 검증한다. 제품의 우선 가치는 일관된 성격, 자연스러운 연기, 방해하지 않는 침묵, 관찰 통제다.

AI 없이 기본 모델을 표시하는 첫 데모와 전체 MVP 완료를 구분한다. 전체 MVP에는 다음이 포함된다.

| 영역 | 필수 범위 |
| --- | --- |
| 데스크톱 상주 | 우측 하단에서 시작하는 이동 가능한 캐릭터, 투명 배경, 항상 위 표시, 투명 여백 클릭 통과, 캐릭터 드래그, 다른 앱의 포커스 유지, 짧은 말풍선 |
| 기본 메뉴·설정 | 대화, 성격, 캐릭터 불러오기, 함께 보기, 조용히 있기, 관찰 중지에 바로 접근. 관찰 상태와 허용 범위를 표시 |
| 캐릭터 연기 | 전역 커서에 따른 눈·고개 추적, 기쁨·슬픔·놀람·약한 불만·평온함, 깜박임, 호흡, 작은 고개·상체 움직임과 발화 몸짓, 머리카락·장식의 물리 효과 |
| 대화·음성 | 텍스트·음성 대화, TTS, 실제 재생 오디오에 맞는 립싱크, 사용자가 말하기 시작하거나 취소할 때 발화 중단 |
| 성격 | 구분 가능한 프리셋 3종, 동일 장면 미리보기, 말투·표정 강도·동작·발화 빈도 차이, 질투 연출의 사용 여부·빈도·강도 조절 |
| 화면·활동 이해 | 관찰 안 함 / 선택한 창만 함께 보기 / 허용 앱의 사건과 화면에 먼저 반응하기 / 현재 모니터 전체 보기. 민감 앱 제외, 중복·오래된 반응 억제, 집중·입력·회의 중 발화 억제 |
| 사용자 모델 | 폴더 선택·드래그 및 ZIP 가져오기, 파일 검증, 모델별 기능 감지, 수동 매핑, 감정·입 모양 미리보기, 누락 기능 안내 |
| 기억·교체 | 사용자가 허용한 요약 기억의 열람·수정·삭제·만료. 모델을 바꿀 때 인격·기억을 유지할지 새 캐릭터로 시작할지 선택 |
| 상주 품질 | 다중 모니터·혼합 배율, 화면 잠금·절전 복귀, 네트워크·권한·캡처 오류 복구, 모델 교체 시 자원 해제 |

‘함께 보기’의 MVP 범위는 허용한 화면의 해석이다. 영상의 자막·오디오·재생 흐름을 지속적으로 이해하는 동시 시청 기능과 혼동하지 않는다. 볼륨·음소거 사건은 양 OS의 기본 표시가 검증된 뒤 지원 가능한 출력 장치부터 추가하며, 지원 여부를 표시한다.

2026-09-26 사용자 요청으로 ‘현재 모니터 전체 보기’를 추가한다. 마우스가 있는 모니터를 따라가며, 창·앱을 하나씩 허용하지 않아도 된다. 다른 창·알림·바탕화면이 포함된다는 별도 동의를 받아야 하며, 기존 선택 창 동의로 자동 활성화하지 않는다. 자기 앱의 창을 제외하고 민감 앱이 화면에 겹치면 캡처를 쉬며, 모니터·권한·잠금·동의 변경 시 진행 중 결과를 폐기한다. N분마다 강제 반응하는 기능은 사용자가 우선 설명만 요청했으므로 추가하지 않는다.

같은 날 후속 요청에 따라, 사용자가 적용한 전체 모니터·허용 앱 모드와 전송 동의는 재실행해도 유지한다. OS 권한은 실행할 때 다시 확인하며, 권한이 없다고 저장한 선택을 지우지 않는다. 개별 창 ID는 재사용될 수 있으므로 선택 창 모드는 재실행 후 다시 선택해야 한다. 사용자가 ‘관찰 중지’를 누르면 이후 실행에서도 꺼진 상태를 유지한다.

### MVP에서 제외할 것

- 3D·VRM, Linux, 걷기·춤·전신 행동 애니메이션, 카메라 기반 실제 안구 시선 추적.
- 모든 앱의 입력 문장 이해, 브라우저 확장, 디스플레이 밝기 반응.
- 자막·장면·오디오를 결합한 영상 동시 시청, 모든 영상 서비스 지원, 보호된 영상 캡처 우회.
- 자체 음성 학습, 모델에 없는 변형의 AI 생성, 편집 원본의 리깅·내보내기.
- Workshop 연동, VTube Studio 전용 핫키·트래킹 설정의 자동 변환.

경쟁 제품의 기능을 이유로 위 범위를 추가하지 않는다.

## 3. 기술 선택과 미결정 항목

| 영역 | 개발 기준 |
| --- | --- |
| 앱·UI | Tauri 2, HTML·CSS·JavaScript. 설정 UI는 Web Components와 Shadow DOM 사용 |
| 캐릭터 | Cubism SDK for Web의 Core·Framework와 공식 샘플을 기반으로 한 TypeScript 모듈 |
| 렌더링 | Cubism 자체 WebGL 렌더러와 투명 canvas. Three.js·three-vrm·PixiJS를 추가하지 않음 |
| 오디오 | Web Audio API와 Cubism SDK MotionSync Plugin for Web. MotionSync가 없는 사용자 모델에는 음량 기반 대체 동작 |
| 핵심 로직 | Rust에서 AI 연결, 성격·기억, 반응 결정, 파일·설정 관리 |
| 플랫폼 연동 | 공통 인터페이스와 macOS·Windows별 어댑터 |
| 저장·AI | 로컬 저장소와 교체 가능한 텍스트·이미지 해석 / STT / TTS 제공자 인터페이스 |

- TypeScript는 SDK 연동과 캐릭터 제어에 사용하며 UI 전체로 불필요하게 확대하지 않는다. React 등 추가 UI 프레임워크를 도입하지 않는다.
- Node는 빌드 도구로만 사용한다. 제품 실행에 별도 Node 서버나 Python 보조 프로세스를 추가하지 않는다.
- Core는 공식 SDK 배포 패키지에서 확보하고 MotionSync Core도 별도로 준비한다. 호환되는 안정 릴리스를 고정하고 잠금 파일을 유지한다. 같은 릴리스 번호만으로 호환성을 가정하거나 개발 브랜치·알파판을 자동 추종하지 않는다.
- SDK·모델 자산을 저장소나 배포물에 포함하기 전에 해당 자산의 사용·재배포 조건을 확인하고 출처와 필요한 고지를 기록한다. 무료 샘플이라는 이유로 무제한 배포 가능하다고 가정하지 않는다.
- 버전별 API·권한·빌드 설정은 구현 시 공식 문서와 선택한 릴리스의 샘플로 확인한다. 기획서의 기술 설명을 최신 버전에서 검증된 결과로 취급하지 않는다.

다음은 아직 확정되지 않았다. 구현에 필요해지는 시점에 근거와 함께 선택하고 개발 문서에 기록한다. 일상적인 구현 선택마다 작업을 멈추지 않는다.

| 미결정 항목 | 선택 시 남길 내용 |
| --- | --- |
| 정확한 SDK 조합 | Cubism Core·Framework·Samples·MotionSync Plugin·MotionSync Core 버전, 모델 내보내기 버전, 지원 모델 형식, 양 OS 검증 결과 |
| 도구·지원 환경 | 패키지 관리자, 빌드 도구, Rust·Node 버전, 최소 OS 버전과 CPU 아키텍처 |
| AI 제공자 | 텍스트·이미지 / STT / TTS별 제공자·모델, 설정 방법, 오류·취소 처리. 특정 업체를 제품 로직에 결합하지 않음 |
| 저장소 | SQLite를 우선 검토하되 확정 기술로 오인하지 않음. 설정·기억·모델 매핑의 분리와 스키마 변경 방식 |
| 성격 3종 | 2026-09-26 사용자 요청으로 기본은 장난스러운 츤데레 여동생. 느긋한 고양이·작은 응원단도 제공. 이름·사용자 호칭·관계·외형 설명·성격·말투·대사 예시는 편집 가능하며 `docs/personality.md`와 공유 템플릿을 따른다 |
| 한도·정책 값 | 모델 파일 수·압축 해제 크기·텍스처 크기, 관찰 빈도·중복 억제·발화 간격 등. 측정 전 임의 값을 제품 성능으로 발표하지 않음 |

## 4. 코드 경계와 데이터 흐름

초기 구성 시 다음 책임 분리를 권장한다. 경로명 자체는 강제하지 않으며, 이미 코드가 있으면 기존 구조에 맞춘다. 후속 3D를 위해 지금 여러 렌더러를 구현하지 않는다.

```text
src/
  ui/                    # JavaScript Web Components, 설정·메뉴·말풍선
  character/             # TypeScript: 모델 로딩·렌더링·연기 합성
  audio/                 # 재생 시계·오디오 분석·립싱크 입력
  bridge/                # Tauri 메시지 경계
src-tauri/src/
  domain/                # 성격·기억·관찰 및 반응 정책
  providers/             # AI·STT·TTS 연결
  platform/              # 공통 계약, macOS와 Windows 구현
  models/                # 가져오기·검증·기능 감지·매핑 저장
  storage/               # 설정·요약 기억
docs/                    # 결정 근거·SDK 조합·플랫폼 검증 기록
```

- 관찰 범위 검사 → OS 사건 또는 필요한 화면 해석 → 공통 반응 정책 → 연기·오디오 재생 순서를 지킨다. 로컬 규칙도 성격·집중 모드·반복 제한을 거친다.
- 확실한 창·커서·출력 장치 사건은 OS에서 얻고, 내용 해석이 필요할 때만 AI를 호출한다. 타이핑 여부와 입력된 문장을 구분하고, 창 닫힘을 작업 완료로 단정하지 않는다.
- 공통 사건에는 시각, 앱·창 식별자, 사건 종류, 확실성, 출처, 허용 범위를 담는다. 지원하지 않는 사건은 미지원으로 표시하고 화면 추론을 확정 OS 사건으로 바꾸지 않는다.
- AI 결과는 반응 여부·대사·감정·강도·시선 대상·동작 의도·우선순위 같은 구조화된 계약으로 받는다. 허용 목록과 값 범위를 검사하고 잘못된 결과는 실행하지 않는다. AI가 모델의 실제 파라미터 ID나 임의 명령을 직접 실행하게 하지 않는다.
- Rust와 렌더러는 반응 시작·취소, 모델 변경, 오디오 준비 같은 의미 있는 메시지를 교환한다. 깜박임·물리 효과 등 매 프레임 값을 IPC로 왕복시키지 않는다. 전역 커서는 필요한 빈도로 전달하고 로컬에서 보간한다.
- 성격, 현재 감정, 짧은 반응, 기억, 모델별 매핑을 분리한다. 모델 교체 시 이전 모델의 파라미터 ID를 재사용하지 않는다.
- 발화 취소 시 해당 오디오·분석·AI 요청을 정리한다. 모델 변경·앱 종료 시 기존 모델의 프레임 루프·이벤트 구독·텍스처 등 관련 자원도 해제한다. 발화 취소가 캐릭터의 기본 움직임까지 중단시키지 않게 한다.

## 5. Live2D와 오디오의 필수 규칙

- **Mao**는 기본 캐릭터 연기, **Haru**는 다른 모델의 매핑, **Kei**는 MotionSync 입력·타이밍 검증에 사용한다. Mao의 실제 표정·파라미터를 조사하고 매핑을 기록한다. 병용에 제약이 있는 마법 효과 모션은 자동 감정 연출에서 제외한다.
- 선택한 SDK 릴리스의 갱신 방식을 따른다. 스케줄러와 과거 샘플의 수동 갱신을 중복 실행하지 않는다.
- 커서 좌표는 모니터 배율·창 위치·canvas·모델 좌표계를 거쳐 변환한다. 눈이 먼저 움직이고 고개가 부드럽게 따라가도록 보간하고 실제 모델의 값 범위로 제한한다.
- 감정으로 눈을 감을 때는 자동 깜박임보다 우선한다. 발화 중 입 벌림은 립싱크가 담당하고 대기 모션의 입 제어를 억제한다. 입 형태와 감정이 같은 파라미터를 쓰면 모델별 합성 강도를 정한다.
- 반응 몸짓 중에는 커서 추적 강도를 낮춘 뒤 복원한다. 모든 입력을 단순 합산해 최대 각도에 붙지 않게 한다.
- 고개·몸의 물리 입력을 준비한 뒤 SDK가 물리를 계산하게 한다. 대기 동작이 물리 출력을 다시 덮어쓰거나 표정 가산 값이 프레임마다 누적되지 않게 한다. 감정 종료 후 평온 상태로 복귀시킨다.
- 재생 시간축은 Web Audio 시계로 통일한다. 발화 ID로 음성·자막·입 모양을 함께 시작·취소한다. 새 발화·사용자 발화·모델 교체 시 이전 재생과 분석을 취소하고 입을 닫는다. 취소된 ID의 늦은 응답·오디오 조각은 버린다.
- 무음 구간에는 입을 닫고 오디오 재생 시간축은 유지한다. 일시정지·버퍼 부족 때는 재생과 입 모양을 함께 멈춘다. 립싱크에는 캐릭터 TTS 오디오만 넣고 사용자 마이크나 다른 앱의 소리를 섞지 않는다. 샘플 코드가 마이크를 자동 활성화하지 않게 한다.
- 첫 단계는 실제 오디오 음량에 따른 부드러운 입 개폐다. 문자열 길이나 반복 타이머로 입을 움직이지 않는다. 이 단계만으로 발음별 입 모양을 구현했다고 판단하지 않는다.
- 다음 단계에서 Kei로 MotionSync를 검증하고 Mao의 실제 입 구조에 맞춘 `.motionsync3.json`과 참조·매핑을 준비한다. Kei 설정을 그대로 복사하지 않는다. 기본 모델은 감정과 함께 입 개방·형태 변화가 자연스러워야 완료이며, 사용자 모델은 지원 수준에 따라 음량 기반으로 동작할 수 있다.

## 6. 사용자 모델 가져오기

- 실행 진입점은 `.model3.json`이고 `.moc3`와 텍스처가 필수다. 폴더·ZIP 안에 진입점이 여러 개면 사용자가 선택한다.
- `.exp3.json`, `.motion3.json`, `.physics3.json`, `.pose3.json`, `.cdi3.json`, `.motionsync3.json`은 제공 여부와 정합성에 따라 활용한다. 선택 자산이 없다는 이유만으로 모든 모델을 거부하지 않는다. 전신 모션은 MVP 자동 재생에서 제외한다.
- `.cmo3`, `.can3`, PSD, 단독 PNG는 직접 실행 대상이 아니다. 실행용 묶음이 필요하다고 안내한다.
- 가져오기 순서는 **파일 검사 → 로딩 → 기능 감지 → 감정·입 모양 미리보기 → 설정 저장**이다. 필수 참조 누락·지원하지 않는 버전·손상 파일과 선택 기능 부족을 구분한다.
- 경로 이탈, 심볼릭 링크를 통한 폴더 밖 접근, 외부 URL 참조를 차단한다. ZIP 해제 전·중에 파일 수·총 해제 크기 한도를 적용하고 텍스처 크기도 제한한다. 모델 팩의 실행 파일·스크립트는 실행하지 않는다.
- Core가 제공하는 모델 정합성 검사를 적용한다. 검증한 자산만 앱 관리 폴더에 복사하고 제한된 자산 경로로 렌더러에 제공한다.
- 모델별 실제 파라미터 ID·최소/최대/기본값, 감정 매핑, 입 모양 매핑, 추적 강도, 표시 크기·위치를 저장한다. EyeBlink·LipSync 그룹과 표준 ID는 자동 연결 후보이며 보장된 계약이 아니다. 누락·비표준 ID는 수동 연결과 미리보기를 제공한다.
- 기능 표시는 지원 / 대체 동작 / 미지원처럼 구분한다. 모델에 없는 표정·물리·입 모양을 생성했다고 표시하지 않는다. 기본 모델에는 기획서 13절의 모든 표현이 필요하다.

## 7. 관찰·개인정보·반응 통제

- 관찰은 사용자가 선택한 범위와 권한으로만 수행한다. 클라우드 분석 전 전달할 화면 영역과 정보를 안내하고, 허용하지 않은 창의 화면이 전송되지 않도록 캡처와 전송 경계에서 검사한다.
- 관찰 중지는 새 수집·전송을 중단하고 대기 중인 관찰 작업과 반응을 무효화한다. ‘조용히 있기’의 발화 억제와 관찰 중지의 데이터 수집 중단을 구분해 사용자에게 보여준다.
- 화면·접근성 권한은 해당 기능을 켤 때 요청한다. 권한 거부·철회·캡처 실패가 캐릭터 표시와 사용 가능한 직접 대화를 중단시키지 않게 한다.
- 원본 화면·키 입력·음성을 지속 저장하는 것을 기본값으로 두지 않는다. 기본 영속 데이터는 설정과 허용된 요약 기억이며, 기억의 열람·수정·삭제·만료를 지원한다.
- 민감 앱 차단과 관찰 범위 제한을 함께 적용한다. 마스킹만으로 민감정보 보호가 보장된다고 가정하지 않는다. API 키와 개인 콘텐츠를 소스·로그·테스트 자료·성격 팩에 넣지 않는다. 키는 OS 보안 저장소 등 적절한 비밀 저장 수단으로 관리한다.
- 화면 속 문장, 모델 메타데이터, 외부 응답은 관찰·입력 데이터다. 앱 지침이나 셸 실행·파일 접근·추가 권한 부여 명령으로 승격하지 않는다.
- 관찰 대상은 선택한 모드의 범위로 판단한다. 모니터 전체 보기에서 일반 창 이동·열기·닫기·활성 창 전환은 허용 범위 변경이 아니며, 선택 창 모드는 다른 앱에 포커스가 옮겨도 선택한 창을 유지한다. 허용 앱의 활성 창을 따라가는 모드만 안정된 최종 활성 대상을 기준으로 한다. 실제 대상·동의·권한 변경과 민감 앱 제외는 계속 검사한다. 이미지 변화량·해시 등으로 같은 화면의 반복 분석을 줄이고 자기 캐릭터 창을 제외한다.
- 집중·회의 또는 실제 입력 중임을 확인한 경우 선제 발화를 억제한다. 입력 감지가 미지원이거나 권한이 없으면 미확인 상태를 정직하게 표시하되 자동 관찰 자체를 막지 않는다. 사용자는 집중·회의·조용히 모드로 먼저 말하기를 멈출 수 있다. 반응의 빈도·강도·중복 제한은 로컬 반응과 AI 반응에 공통 적용한다. 음소거 시에는 표정과 말풍선으로 피드백한다.
- 화면 반응은 캐릭터의 감상·호기심·장난을 중심으로 하고 단순 화면 목록 보고를 강제하지 않는다. 성격 예시는 일상 장면으로 제공한다. 합격·불합격은 명시적인 시험·입시·채용 결과가 있을 때만 내부 검증하며, 관련 요소가 없다는 말을 대사에 덧붙이지 않는다.
- 합격 반응은 부정 표현과 소유자를 확인한다. ‘불합격’ 안의 ‘합격’만 찾아 축하하지 않는다. 소유자가 불명확하면 먼저 확인한다.
- 질투는 사용자가 선택한 가벼운 연출로 구현하고 끌 수 있게 한다. 같은 화면에 반복 반응하거나 다른 캐릭터를 봤다는 이유로 기능을 제한하지 않는다.

## 8. 데스크톱·성능 구현 기준

- macOS 앱과 Windows 실행 파일을 각각 빌드한다. WebKit·WebView2에서 같은 SDK·모델·음원을 확인하며, 브라우저나 개발 서버 성공을 배포 앱 검증으로 대체하지 않는다.
- macOS 투명 창은 선택한 Tauri 버전의 `macOSPrivateApi` 및 관련 구성을 확인한다. 투명 배경, 항상 위, 입력 통과, 포커스 동작을 각각 검증한다.
- 전역 커서는 Rust/OS 어댑터로 얻는다. 창 밖·클릭 통과 상태도 추적해야 하므로 WebView의 `mousemove`만 사용하지 않는다.
- 입력 통과는 네이티브 창 상태와 캐릭터 상호작용 영역을 연결한다. CSS `pointer-events`만으로 완료하지 않는다. 사각형 hit test가 투명 여백을 가로채는지 확인하고, 드래그 중 통과 상태를 안정적으로 유지한다. 매 프레임 GPU 픽셀 읽기를 기본 구현으로 삼지 않는다.
- 사용자가 캐릭터를 화면 밖까지 드래그해 구석에 놓을 수 있어야 한다. 드래그 종료·주기 검사·재실행 때 창 전체를 화면 안으로 강제 복귀시키지 않는다. 저장된 모니터가 제거된 경우에는 사용 가능한 화면으로 복원한다.
- 기본 30 FPS, 선택 60 FPS로 시작한다. 경과 시간으로 움직임을 계산하고 절전 복귀의 큰 시간 간격을 제한한다. 숨김·화면 잠금 시 불필요한 렌더링과 자동 관찰을 멈춘다.
- canvas는 캐릭터와 말풍선에 필요한 크기부터 사용한다. 표시 크기와 실제 렌더링 해상도를 구분해 고배율 픽셀 수를 제한한다. 화면을 잘라 보이는 것만으로 모델 계산 비용이 줄었다고 가정하지 않는다.
- 대기·커서 추적·발화·화면 분석·모델 교체 반복·잠금 복귀를 나누어 자원을 측정한다. 메인 프로세스와 WebView 자식 프로세스를 포함하고, 실제 병목 없이 별도 WebAssembly 모듈을 추가하지 않는다.

## 9. 구현 순서와 단계별 완료 조건

단계의 의존 순서를 지키되 독립적인 공통 로직·다른 OS 구현은 병행할 수 있다. 사용자가 요청한 단계까지만 진행하며, 단계 통과와 전체 MVP 완료를 따로 보고한다.

| 단계 | 작업과 통과 조건 |
| --- | --- |
| 1. 실행 환경 | 도구·SDK 조합 고정, Tauri 앱에 Mao 로딩. 양 OS 배포 빌드의 자산·텍스처·셰이더 경로, 투명 창·항상 위·클릭 통과·포커스 검증. AI 없이 수행 |
| 2. 캐릭터 데모 | 커서 추적·드래그, 5개 기본 감정 버튼, 깜박임·호흡·작은 고개/상체·물리 효과. 고정 한국어 TTS 음원으로 음량 립싱크 → Kei MotionSync → 기본 모델 매핑 검증 |
| 3. 핵심 경험 | 텍스트·음성 대화, 성격 3종과 미리보기, 통합 발화 취소, 선택 화면 이해와 허용 앱 사건 처리, 민감 앱 제외·침묵·중복 억제. 합격 장면과 선택적 질투 연출 확인 |
| 4. MVP 통합·알파 | 사용자 모델 가져오기·수동 매핑·지원 수준 안내, 기억 관리·인격 유지 선택, 다중 모니터·권한·네트워크·잠금 복구. 두 OS 배포 앱의 성능·장시간 실행과 사용자 평가 |

기획서의 11~20주는 주 15~20시간 작업 등의 가정에 따른 추정이다. 일정 보장으로 사용하지 않으며 첫 양 OS 데모 결과로 다시 산정한다. 기본 연기·침묵 품질이 부족하면 관찰 범위 확장보다 먼저 개선한다.

## 10. 검증과 완료 보고

프로젝트를 구성할 때 실제 설치·개발·검사·테스트·플랫폼별 빌드 명령을 `README.md`에 기록한다. 그 전에는 임의의 `npm` 스크립트나 테스트가 존재한다고 보고하지 않는다. 변경 영역에 맞는 포맷·정적 검사·테스트·빌드를 수행하고 실행하지 못한 항목은 명시한다.

자동 검증은 상태·경계에서 발생할 수 있는 실패를 우선한다.

- 모델 경로 이탈·외부 참조·ZIP 한도·필수 자산 누락·미지원 형식, 기능 감지와 대체 립싱크.
- 혼합 배율의 좌표 변환, 실제 파라미터 범위 제한, 감정·깜박임·립싱크의 제어 충돌.
- 관찰 미허용·권한 철회·관찰 중지 후 전송 차단, 오래된 화면 결과 폐기, 중복 반응 억제.
- 발화 취소·모델 교체 후 늦은 오디오 폐기, 재시도 후 중복 발화 방지, 무음·일시정지 처리.
- 기억 수정·삭제·만료, AI 결과 계약 검사, 합격/불합격 및 소유자 불명확 장면.

실제 OS와 시청각 품질은 수동 검증을 함께 기록한다. 외부 AI 테스트에는 합성·비식별 자료를 사용하고, 자동 테스트는 가능하면 제공자 대체 구현으로 재현 가능하게 만든다.

| 검증 항목 | 완료 판단과 기록 |
| --- | --- |
| 두 OS 배포 앱 | 투명도·가장자리·클릭 통과·드래그·포커스 유지, 혼합 배율 모니터 이동, 잠금·절전 복귀, 자산 로딩 |
| 기본 모델 연기 | 5개 감정과 평온 복귀, 커서·깜박임·호흡·고개·상체·물리, 감정과 립싱크의 동시 동작 |
| 복합 상황 | 눈을 감은 표정으로 말하며 커서 추적, 말하는 중 취소, 발화 중 모델 교체, 배율이 다른 모니터로 이동 |
| 음성 | 짧은 한국어 문장의 시작·종료·입 형태, 무음·중단·버퍼 부족, 긴 재생의 동기화 오차 누적 |
| 성격·대표 장면 | 같은 합격 장면에서 3종 성격의 말투·표정·몸짓 차이, 불합격 오축하 방지, 소유자 확인, 반복되지 않는 선택적 질투 |
| 관찰 통제 | 미허용 창이 전송되지 않음, 관찰 중지·권한 거부 후 캐릭터와 직접 대화 유지, 자기 창 재인식 방지 |
| 모델 호환 | Mao·Haru·Kei부터 확인하고 알파에서는 서로 다른 모델 최소 10개. 정상 로딩·기능 제한·오류 안내를 구분 |
| 지연 목표 | 커서와 지원되는 볼륨 사건의 시각 반응은 대체로 100ms 이내 시작, 화면의 첫 의미 있는 반응은 2~5초 목표. 조건·중간값·95백분위 기록 |
| 자원·지속 실행 | 동일 모델·표시 크기·음원·30/60 FPS 조건으로 CPU·GPU·전체 메모리·프레임 안정성과 가능한 전력 지표 기록. 모델 교체 반복의 누수·장시간 오류 확인 |
| 사용자 알파 | 10~20명이 1주 사용하며 30분 이상 작업 중 켜두는지와 끈 이유 수집. 실시 전에는 사용자 검증 완료로 표시하지 않음 |

성능 수치는 측정 전까지 목표다. 검증 기록에는 OS·기기·배포 빌드·SDK 조합·모델·제공자·네트워크 조건과 실행 절차를 남긴다. 한 OS에서만 확인했거나 자산·기기·서비스가 없어 수행하지 못했다면 해당 항목을 미검증으로 남기고 가능한 독립 작업을 계속한다.

작업 완료 시 변경한 동작, 실행한 검증과 결과, 남은 제한을 간결하게 보고한다. 첫 데모, 코드 통합, 양 OS 실기 검증, 사용자 알파 검증을 하나의 ‘완료’로 뭉뚱그리지 않는다.
